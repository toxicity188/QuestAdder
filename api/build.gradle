afterEvaluate {
    publishing {
        publications {
            paidRelease(MavenPublication) {
                groupId = this.group
                artifactId = 'final'
                version = this.version
            }
            paidDebug(MavenPublication) {
                groupId = this.group
                artifactId = 'final-debug'
                version = this.version
            }
        }
    }
}